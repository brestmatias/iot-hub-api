# Server configurations
server:
 port: 8080
 ginMode: "release"
 verbose: false

database:
 uri: "mongodb://localhost:27017"
 db: "home"

station-rest-client:
 baseURL:        "http://"
 connectTimeout: 90
 timeout:       100
 disableCache:   true
 disableTimeout: false

slow-station-rest-client:
 baseURL:        "http://"
 connectTimeout: 100
 timeout:       100
 disableCache:   true
 disableTimeout: false